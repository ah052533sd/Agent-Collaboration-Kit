# 本仓库的工作规则（给 Claude Code / Codex）

本仓库是 Claude ↔ Codex 协作机制的**唯一真相源**，不是使用这套机制的项目。分清楚这一点，下面几条就都自然了。

## 1. 不要在本仓库安装这套机制

不要建 `journal/`、不要装 hook、不要把 `template/journal/bin/*.py` 复制到仓库根目录。**脚本只有 `template/` 里那一份**；装进来就成了第二份，正是这套机制存在要防的漂移。本仓库的过程记录就用 `git log` 和 `reviews/`。

## 2. 改动流向是单向的

```
改 template/ → 同步到消费方项目 → 跑 check-drift.py 确认三处一致
```

消费方项目里的**项目特化**（任务目录名、知识层写入门槛、项目特有条款）**不回流**到模板。模板只放通用部分和两处 `【填空】`。

改完必跑：

```bash
/usr/bin/python3 check-drift.py
```

四项检查：脚本是否逐字节一致、硬约束是否还在、协议节逐行差异、消费方流水的健康度。**「实现漂移」报错必须当场修**——两侧共用一份实现是硬要求，曾经各写一份，半天内漂移出四个 bug，其中一个会让一方的判断永久不被对方读到且不报错。

**进模板前先过三问：** ① 只在一个项目会发生 → 留在那个项目的 `AGENTS.md`，不进模板；② 能用脚本或 hook 保证 → 改代码，不写协议文字（文档说一套、机制跑一套时，赢的永远是机制）；③ 都不是 → 进模板，且必须带一句「为什么」，否则下一个 Agent 会当废话删掉。**默认不进**——模板每一行都是两个 Agent 每次会话的常驻成本，规则越多单条被遵守的概率越低，协议因此从 11 条压到 6 条，别再撑回去。

**同步到消费方项目必须当场 commit**（`[claude] sync protocol from kit`）。留未提交改动会让那边的 Agent 按协议「未提交改动先归属」判为**来源未知**而停下报告用户——等于给消费方埋雷。做不到当场 commit 就先别动那个仓库。

## 3. 发布前审计（本仓库会分享给外部）

任何进入版本控制的内容**不得包含**：真实业务名词、系统名、人名、内部项目名、绝对路径（`/Users/<name>/...`）、token 或凭据。文档和脚本里的示例一律用占位符。

已知不进版本控制：`repos.local.txt`（本机仓库路径）、`reviews/`（周度报告会引用消费方项目的业务内容）。改 `.gitignore` 前先想清楚为什么。

新增示例、README 段落或脚本注释后，跑一遍：

```bash
grep -rn -E "<你项目里的业务词>|/Users/" --include="*.md" --include="*.py" . | grep -v repos.local.txt
```

## 4. 改协议时的连带影响

协议条号被 `template/journal/README.md` 和两份说明书（`SETUP-CLAUDE.md`、`SETUP-CODEX.md`）引用。**合并或重排条目后必须全仓搜一遍「第 N 条」并修正**，否则读者按条号找不到东西。

## 5. Commit

message 用英文，`[claude]` / `[codex]` 前缀标明执行方。只 stage 本次实际涉及的路径，禁止 `git add -A`。**push 必须由用户明确要求。**

## 背景

这套机制为什么长成现在这样、哪些方案被否决过、实测数据是多少，见 [`DESIGN-NOTES.md`](DESIGN-NOTES.md)。动协议之前先读它——很多看起来冗余的条款是防漂移条款，删掉之后下一个 Agent 会把已经权衡过的决定重新推翻一遍。
