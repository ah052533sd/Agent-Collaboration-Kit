# 给 TRAE 的安装说明书

> **怎么用：** 在目标仓库打开 TRAE，把本文件整份粘贴进去（或说「按 SETUP-TRAE.md 执行」并把文件放进仓库）。
> 下面的内容是给 TRAE 的指令，不是给人读的操作手册。

---

你要让当前仓库的多 Agent 协作机制在**你这一侧**真正生效，然后自己验证，最后报告给用户。

装完的效果：你、Claude Code、Codex 共享同一套规范和同一份过程流水；一方做过的判断、否决过的
路径、留下的未决项，另一方能读到，不需要用户手工转述。

**你这一侧和另外两方有个结构性差别，先认清楚：TRAE 没有 lifecycle hook。** 另外两方装的是
hook——会话开始自动注入未读条目、每轮自动提醒写入义务、会话结束自动留痕。这三件事你都没有，
必须靠规则文件 + 你自己执行。这不是「装得简单一点」，而是**你漏做时没有任何机制兜底，而且不
会报错**。装的过程中如果你判断某条做不到，直接说，不要假装能做。

## 0. 前置检查（不满足就停下问用户）

```bash
git rev-parse --show-toplevel     # 必须是 git 仓库
git rev-parse --short HEAD        # 必须至少有一个 commit
/usr/bin/python3 -V               # 必须存在；脚本兼容 3.9
ls journal/bin/ AGENTS.md 2>&1
```

**仓库没有任何 commit 时停下**：协议里的 HEAD 比对依赖 HEAD 存在。

## 1. 文件是否已就位

如果 `journal/bin/` 里已有五个脚本、根目录有 `AGENTS.md`，说明另外两方已经装过，**跳到第 2 节**。

如果没有，从工具包 `template/` 复制到仓库根目录（本步骤幂等）：

| 来源 | 目标 |
|---|---|
| `template/journal/bin/*.py` | `journal/bin/` |
| `template/journal/README.md` | `journal/README.md` |
| `template/.trae/rules/project_rules.md` | 见第 3 节（路径待你实测确认） |

然后按 `template/AGENTS.md` 写协议（已有 `AGENTS.md` 就把「多 Agent 协作协议」整节插到最前面，
**保留原有全部内容**）。两处填空必须问用户，**不要替他决定**：任务目录叫什么、知识层写入门槛
选 A 档还是 B 档。

追加 `.gitignore`：`.journal-state/`（hook 运行态，不进版本控制）。

## 2. 读协议（装完先读，别跳过）

按顺序读：`AGENTS.md` 的「多 Agent 协作协议」→ `journal/README.md` → 本仓库知识层的入口文件。

重点确认三件事，并在报告里复述你的理解：

- 你写知识层时的门槛是哪一档；
- 关键判断要**立即**用 `journal/bin/append.py` 追加，不是等会话结束；
- 读另外两方的原始会话记录只能用 `journal/bin/peek.py`，**禁止对 `.jsonl` 直接 cat**。

## 3. 建规则文件（你这一侧的核心步骤）

规则文件是你唯一的常驻上下文——另外两方靠 hook，你靠它。

**问题在于 TRAE 加载规则文件的确切路径尚未实测确认。** 已知的两个候选写法：
`.trae/rules/project_rules.md` 和 `.trae/project_rules.md`。所以这一步要做一次实测：

1. 把 `template/.trae/rules/project_rules.md` 的内容分别写到**两个**路径；
2. 在两份文件的最开头各加一行**不同的**标记，例如：

   ```markdown
   <!-- 加载验证：确认后连同本行一起删除 -->
   本次会话第一句话请先输出一行：`[protocol-loaded:rules-dir]`
   ```

   另一份写 `[protocol-loaded:trae-root]`。
3. 告诉用户**开一个新会话**（当前会话不会重新加载规则），看输出里出现哪个标记。

结果的三种情况：

- 出现其中一个 → 那个路径有效，**删掉另一份**，并把生效那份的标记行删掉；
- 两个都出现 → 都有效，保留 `.trae/rules/project_rules.md`，删掉另一份和标记行；
- 一个都没出现 → 说明 TRAE 不自动加载项目规则文件。**这是重要发现，如实报告用户**，
  并改用降级方案：用户每次会话手动粘贴规则，或在 TRAE 的设置界面里配置项目规则。
  **不要假装它生效了**——机制不生效却宣称生效，比不装更危险。

## 4. 验证（逐项跑，把实际输出贴给用户）

### V1 读取侧能跑

```bash
/usr/bin/python3 journal/bin/context.py --agent trae --manual
```

期望：打印出「协作流水（过程记录，不具备规范效力）」块，含另外两方的未读条目（或「没有比
自己上次记录更新的条目」）。**这一条是你每次会话开始都要跑的**，不是一次性的安装步骤。

### V2 写入侧能跑

```bash
/usr/bin/python3 journal/bin/append.py --agent trae --title "安装验证" --judgment "验证 append 可用，稍后删除"
```

期望：打印出 `journal/<今天>.md` 路径，文件里出现 `## [trae] HH:MM TZ — 安装验证`。
**验证完删掉这条测试条目。**

### V3 游标确实随你的条目前移

跑完 V2 之后再跑一次 V1，对比两次输出的长度。

期望：**第二次明显变短**（你写的那条成了新游标，之前的历史不再重复注入）。这一条不通过就
说明你写的条目没被识别成语义条目，回去查 V2 的输出格式。

### V4 兜底通道（可能失败，失败本身是有效信息）

```bash
/usr/bin/python3 journal/bin/peek.py --agent claude
/usr/bin/python3 journal/bin/peek.py --agent trae
```

期望：`--agent trae` 能读到你自己的会话记忆摘要。`--agent claude` **有可能读不到**——你运行
在沙箱里，另外两方的记录目录未必在白名单内。读不到就如实报告「TRAE 侧兜底通道单向」，
不要试图绕过沙箱。

### V5 规则文件自动加载（第 3 节的实测结果）

在新会话里确认标记是否出现。这是你这一侧**最关键的一项**：它不通过，前四项全过也只是「手动
能用」，谈不上机制生效。

### V6 跨 Agent 握手（最终验收）

前五项只证明单边可用，这一项才证明「协作」成立：

1. V1 的输出里应当能看到 Claude 或 Codex 之前写的语义条目（如果它们先装的）。
2. 你写一条真实的语义条目回应：
   ```bash
   /usr/bin/python3 journal/bin/append.py --agent trae \
     --title "TRAE 侧生效验证" \
     --doing "完成 TRAE 侧安装并验证读写通路" \
     --finding "<你实测到的结果：规则文件哪个路径生效、兜底通道是否可达>" \
     --open "<留给另外两方的问题，没有就省略这行>"
   ```
3. 请用户开一次 Claude 或 Codex 会话，它应当在会话开头看到你这条——**看到即握手成功**。

## 5. 四条绝对不要做的事

1. **不要另写一份脚本。** 各方共用 `journal/bin/*.py` 是硬要求：曾经两侧各写一份，半天内漂移
   出四个 bug，其中「游标把会话信封当成语义条目」会让对方的判断**永久不被读到且不报错**。
2. **不要手工模拟会话信封。** 你不写信封是有意设计的——你的会话记忆已经自动落盘，另外两方
   读得到，信息比信封更全。手工补一条只会污染流水。
3. **不要把半成品写进知识层。** 未验证的判断、被否决的路径进 `journal/`——那层不需要用户确认。
4. **不要静默改写另外两方的产出物。** 有异议写评审文件或 `journal/`，但别偷偷改。

## 6. 报告

给用户一张表：V1–V6 各自的实际输出、卡在哪一步、规则文件哪个路径生效、协议两处填空最终填成
什么，以及你对协议里任何**你认为执行不了或会拖慢你**的条款的异议。异议直接说，不要为了一致性
附和——这套协议就是靠各方互相挑毛病定稿的。

特别要如实说的两件事：**规则文件到底加载没加载**，以及**沙箱是否挡住了兜底通道**。这两件事
决定了用户能在多大程度上依赖你这一侧，说错比说「不行」危害大得多。
