# 设计笔记：为什么长成这样

这套机制是在两个真实项目上边用边改出来的，不是设计出来的。下面记录每个关键选择的理由、被否决的方案，以及实测数据——**动协议之前先读这份**，很多看着冗余的条款是防漂移条款，删掉之后下一个 Agent 会把已经权衡过的决定重新推翻一遍。

## 要解决的问题

两个 Agent 各写各的，用户就变成了人肉同步管道：跟 A 讨论完的结论要手工粘给 B；A 否决过的方案，B 过两天又提一遍。

更关键的是**结论能转述，判断过程转述不了**。知识层按主题收敛已确认结论，任务目录按任务保存产物，两者都会过滤掉「试过什么、否决了什么、为什么放弃」。而多 Agent 协作恰恰需要过程——另一方要能复核你的判断，而不只是接受你的结论。

所以加了第三层：`journal/`，唯一按时间、跨任务、允许写半成品的层。

## 三层结构

| 层 | 装什么 | 规范效力 | 写入门槛 |
|---|---|---|---|
| 知识层（`knowledge/` 等） | 已确认结论，按主题收敛 | 有 | 高，需用户确认 |
| 任务目录 | 产物与证据，按任务隔离 | 有 | 中 |
| `journal/` | 过程判断、否决路径、未决项 | **无** | **无需确认** |

`journal/` 无规范效力这一点必须写死：允许写半成品是它的价值所在，但引用时必须标明是过程记录，否则未验证的判断会被当成项目事实。

## 两侧共用一份实现（最重要的一条）

最早的版本是 Claude 侧和 Codex 侧各写一份 hook 实现。**半天之内漂移出四个 bug**：

1. **游标把会话信封当成语义条目**——一方的判断永久不被另一方读到，**且不报任何错**。触发条件：A 的信封落在 B 的语义条目之后（开着窗口不关、中途切去用另一个 Agent）。
2. **注入不过滤信封**——实测单次注入 6120 字符里 24 条是信封、1 条是真内容，且已撞上 6000 字符上限被从中间截断。修好后同一份数据是 1365 字符、0 信封、1 条完整语义。
3. **空转判定按天失效**——判据是「今天这个文件里有没有本方语义条目」，当天写过第一条之后恒为真，之后每个会话都写信封。实测一天堆出 25 条信封对 4 条语义。
4. **对方写 journal 触发本方写信封**——`journal/` 的改动被算成「本次会话干过活」。

四个里三个直接损害协作本身。结论：**两侧共用 `template/journal/bin/*.py`，只差 `--agent` 一个参数**。这是结构性消除，不是省事——两份实现一定会漂移，区别只是多久。

`check-drift.py` 的第一项检查就是逐字节比对，报错必须当场修。

## hook 能保证什么，不能保证什么

- **能**：可发现性。会话开始注入未读条目，会话结束写一条单行信封（HEAD、未提交改动、原始记录路径）。
- **不能**：语义摘要。`SessionEnd` 触发时会话已经结束，取不到模型，没法总结。

所以分工是：hook 保证「发生过、在哪」，Agent 按协议保证内容，原始会话记录兜 Agent 的漏写。**这个边界绕不过去**，别指望 hook 自动生成判断。

## 成本控制的四条硬约束

1. **游标 + 过滤信封**：只注入对方比自己上次**语义**条目更新的内容；信封不算游标、不进注入，因此注入量不随会话数增长。
2. **注入上限**：单文件 6000 字符、最多 3 天；截断和省略都显式标注，提示 Agent 主动补读，而不是误以为看全了。
3. **每轮提醒约 280 字符**：只说「有关键判断就立即追加」。20 轮约 5.6k，比一次性全量注入还少。
4. **原始记录一律走抽取**：`peek.py` 只抽 user / assistant 消息。实测三个点：135 MB → 51 KB（0.04%）、881 KB → 4.3 KB（0.5%）、449 KB → 4.8 KB（1.1%）。**135 MB 那个直接 `cat` 会当场撑爆上下文**，所以「禁止对 jsonl 用 Read / cat」是硬约束不是建议。

## 会话中途的追赶：不是轮询

`SessionStart` 每会话只跑一次，长会话里对方后写的条目本来看不到。解法不是加轮询或文件监听——那会把「信箱」变成「心跳」，制造真正的耦合。

实际做法：**让本来每轮就在跑的 `UserPromptSubmit` hook 顺带比对一次本会话水位**（`.journal-state/<agent>/<session>.seen`）。有新条目就带进来，水位随之前移，同一条不重复；自己的条目和信封不带。不新增任何触发点，实测每轮仍是 40–50 ms。

## 接力模式 vs 双视角模式

一开始按「两个独立视角、互不修改产出物」建模，结果发现真实场景是**接力**：token 耗尽需要另一方接着做下一步；或一方结果不满意，另一方接着看、不从头开始。按「互不修改」的规则，报告写一半换手就只能另起一份——荒谬。

所以分成两种模式，由用户指定：

- **接力模式（最常用）**：允许在对方产出物上往下做。四条件——用户给了换手指示、动手前归档快照、`manifest.md` 记接力点、**只往下做不重写对方已完成的部分**。
- **双视角模式**：用户明确要两版对照时，各自署名并列，互不修改，合并权在用户。

两种模式共同的底线是**静默改写永远禁止**。它是唯一真正破坏协作的动作——用户会失去判断依据。有异议写评审文件或 `journal/`，但别偷偷改。

还有一条容易漏的：**接手前先确认换手原因**——做不完（继续原路径）还是做不满意（要换思路）。后者沿原路径修补，等于把失败方向又走一遍，花两份成本买同一个失败。

## 全量注入与锚定：有意的取舍

会话开始就把对方的完整判断推过来，会锚定——第二个跑的 Agent 在动脑之前已经看过对方的推理。对「同一目标各做一版」这种场景，这会让两版相似度偏高。

权衡过三个方案：只注入标题、加独立模式开关（产出前屏蔽对方条目）、保持全量。**选了保持全量**，因为主场景是接力，接力方没有完整 context 根本接不了。锚定风险由「先形成自己的判断再对照、一致时说明是独立得出还是被说服」这条纪律承担。

这个选择写进了协议，并注明**不得以「降低锚定」为由自行收窄注入**——否则下一个 Agent 会出于好意把它改掉。

## 被否决的方案

- **写入锁 / claim 机制**：跨工具的锁做不可靠，只会制造虚假安全感。串行由用户保证。
- **协议放全局**（`~/.claude/CLAUDE.md` + `~/.codex/AGENTS.md`）：① 全局文件不进 git，规则为什么这么定就追不回来、也交不出去；② 没装这套机制的项目会被指挥去调用不存在的脚本；③ Claude 和 Codex 的全局文件本来就是两份，全局化只是把漂移从「项目 × N」换成「工具 × 2」，而且没有 `git diff` 能发现。
- **只靠 git 保留旧版**：依赖「原件已 commit」这个前提，而实测中未跟踪的产物被覆盖即永久丢失。所以接力前要归档快照。
- **修订版用 `report-v2.md` 这类并列命名**：canonical 不唯一，下一个接手的人得猜哪个是准的——比丢文件更难查。并列命名只留给真正的「另一套方案」。
- **Agent 自行批准 Codex 的 hook 信任**：hook 可执行任意命令，自批等于取消这道安全控制。必须用户在界面上点。

## 已知的硬阻塞与环境差异

- **Codex 侧的项目 hook 必须由用户在本机信任**（hash 记录在 `~/.codex/config.toml` 的 `[hooks.state]`）。未信任时 Codex 侧完全不触发，机制退回单边。改 `hooks.json` 一个字，信任就失效、要重批。
- Codex `0.146.0-alpha.3.1` 实测会把 `SessionEnd` hook 的 timeout 上限压到 3 秒，配更大值无效。
- `~/.codex/sessions/` 是跨项目全局目录，`peek.py` 按每个 rollout 首行的 `session_meta.cwd` 过滤本项目——不这样做会把别的项目的会话一起读进来。
- 目标仓库**必须至少有一个 commit**：协议里的 HEAD 比对和信封空转判定都依赖 HEAD 存在。

## 规范的执行力分三档

这决定了什么该写在哪：

| 档 | 载体 | 保证强度 |
|---|---|---|
| 机制强制 | hook、脚本 | **真保证**，Agent 想不执行也不行 |
| 上下文规范 | `AGENTS.md`（自动进上下文） | 大概率，但仍是给模型的指令 |
| 记录 | 决策记录、`journal/` | **零**，不自动进上下文 |

推论有两条。第一，**能用代码保证的绝不只写进文档**——曾经文档写着「空转会话不写信封（已修复）」而代码里那个判定是坏的，结果一天堆出 25 条信封：文档说一套、机制跑一套，赢的永远是机制。第二，**决策记录保证不了执行**，它的价值是防止半年后有人把已经权衡过的事再翻一遍。

同理，`AGENTS.md` 里每条保留「一句为什么」不是冗余——没有理由句，防漂移条款会被下一个 Agent 当成废话删掉。但完整论证、日期和实测数据要下沉到决策记录，因为 `AGENTS.md` 每一行都是每次会话的常驻成本，而且**规则越多，单条被认真遵守的概率越低**（协议因此从 11 条压到 6 条）。
